#include <iostream>
#include <string>
#include <stdio.h>
#include <string.h>
#include <vector>
#include <ctime>
#include <set>
#include <map>
#include <unordered_map>
#include <queue>
#include <algorithm>
#include <cmath>
#include <cctype>
using namespace std;

void solve(int N, vector<int> add) {
    int least = 2;
    for (int i = 0; i<N; i++) {

    }
    cout << least;
}
int main() {
    //freopen("in.txt", "r", stdin);
    int N = 0;
    cin >> N;
    vector<int> add;
    int temp;
    for (int i = 0; i < N; i++) {
        cin >> temp;
        add.push_back(temp);
    }
    solve(N, add);

    return 0;
}